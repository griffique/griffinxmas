/**
 * Checks if two position are equals
 */
import { PositionSummary } from './makePositionObject';
declare const isSamePosition: (current: PositionSummary, next: GeolocationPosition) => boolean;
export default isSamePosition;
