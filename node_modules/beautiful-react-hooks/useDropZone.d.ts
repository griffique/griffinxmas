import { MutableRefObject } from 'react';
import { CallbackSetter } from './shared/types';
export declare type DropZoneState = {
    isOver: boolean;
    onDrop: CallbackSetter<(event: DragEvent) => any>;
};
declare const useDropZone: <T extends HTMLElement>(targetRef: MutableRefObject<T>) => DropZoneState;
export default useDropZone;
