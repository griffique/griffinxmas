import { MutableRefObject } from 'react';
import { UseSwipeOptions } from './useSwipe';
/**
 * A shortcut to useSwipe (with horizontal options)
 */
declare const useHorizontalSwipe: <T extends HTMLElement>(ref?: MutableRefObject<T>, options?: UseSwipeOptions) => {
    swiping: boolean;
    direction?: import("./shared/swipeUtils").Direction;
    alphaX: number;
    alphaY: number;
    count: number;
};
export default useHorizontalSwipe;
