import { Noop } from './shared/types';
/**
 * Returns an object wrapping lifecycle hooks such as `useDidMount` or `useWillUnmount`.
 * It is intended as a shortcut to those hooks.
 */
declare const useLifecycle: <M extends (...args: any[]) => any = Noop, U extends (...args: any[]) => any = Noop>(mount?: M, unmount?: U) => {
    onDidMount: import("./shared/types").CallbackSetter<M>;
    onWillUnmount: import("./shared/types").CallbackSetter<U>;
};
export default useLifecycle;
