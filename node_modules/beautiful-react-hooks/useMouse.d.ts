import { MutableRefObject } from 'react';
import { MouseEventsMap } from './useMouseEvents';
import { MouseStateSummary } from './useMouseState';
/**
 * Returns an array where the first item is the mouse state from the `useMouseState` hook and the second item
 * is the object of callback setters from the `useMouseEvents` hook.
 * It is intended as a shortcut to those hooks.
 */
declare const useMouse: <T extends HTMLElement>(targetRef?: MutableRefObject<T>) => [MouseStateSummary, MouseEventsMap];
export default useMouse;
