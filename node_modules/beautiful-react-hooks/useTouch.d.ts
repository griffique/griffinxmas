import { MutableRefObject } from 'react';
import { TouchEventsMap } from './useTouchEvents';
import { TouchState } from './useTouchState';
/**
 * Returns an array where the first item is the touch state from the `useTouchState` hook and the second item
 * is the object of callback setters from the `useTouchEvents` hook.
 * It is intended as a shortcut to those hooks.
 */
declare const useTouch: <T extends HTMLElement>(targetRef?: MutableRefObject<T>) => [TouchState, TouchEventsMap];
export default useTouch;
