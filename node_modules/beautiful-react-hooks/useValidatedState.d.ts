import { Dispatch, SetStateAction } from 'react';
/**
 * Returns a state that changes only if the next value pass its validator
 */
declare const useValidatedState: <T = unknown, V extends (value: T) => boolean = (value: T) => boolean>(validator: V, initialValue?: T) => [T, Dispatch<SetStateAction<T>>, {
    changed: boolean;
    valid?: boolean;
}];
export default useValidatedState;
