/**
 * Accepts a variable (possibly a prop or a state) and returns its history (changes through updates).
 */
declare const useValueHistory: <T = unknown>(value: T, distinct?: boolean) => T[];
export default useValueHistory;
